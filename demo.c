#include <stdio.h>
#include <string.h>
strlen(str) // do dai 1 chuoi
strcmp(str1,str2)  // string compare : so sanh

 int main(){

    char str[100];
    fflush(stdin);
    gets(str);
    scanf ("%s",str);
    printf("\t chuoi vua nhat: %s\n", str);
    // \0 : ket thuc chuoi . no dung laij
    printf("\t Ki tu thu 2 cua chuoi la: %c\n", str[2]);

    str[5] = '\0'
        printf("%s\n",str);
        for (int i=0; str[i]!='\0' ; i++)
            if (str[i] >= 'a' && str[i] <= 'z')
            (
                str[i]=str
             )
    return 0;
    }

